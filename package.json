{
  "name": "aura-payroll",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "install-all": "npm install"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "programs/*"
  ],
  "devDependencies": {
    "turbo": "latest"
  }
}
