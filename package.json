{
  "name": "aura-payroll",
  "private": true,
  "packageManager": "npm@11.12.1",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "install-all": "npm install"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "programs/*"
  ],
  "devDependencies": {
    "playwright": "^1.60.0",
    "turbo": "latest"
  }
}
